# R2 credentials — used by `npm run ingest` only. Never needed to build the site.
# Create an R2 API token in the Cloudflare dashboard: R2 > Manage API tokens.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=oinam-albums

# Used by `npm run describe` only.
ANTHROPIC_API_KEY=
